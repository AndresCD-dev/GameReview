default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: GameReview_dev

test:
  <<: *default
  database: GameReview_test

production:
  <<: *default
